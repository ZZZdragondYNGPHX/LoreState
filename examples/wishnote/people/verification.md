# 缄愿笔记统一版 2.0 验证

2026-09-07：配套 runtime 更新到本地 0.4.0，schema 为 shared: []、person: 已成事实/持续条件/生效关联。原世界书 UID 0–5 内容与元数据保持一致；构建及离线转换回归通过。浏览器确认 data-lore-person 模板能够解析并识别三个人物栏目。

通用引擎已在独立测试卡验证混合更新、保存、正则、冷档、预设、恢复及手动纠错。此证据不代表缄愿笔记整套剧情已在宿主验收。没有覆盖你正在玩的卡。暂无真实模型、完整卡导出再导入或世界书剧情触发验收。

本版交付组件，远程脚本固定 prototype-v0.4.0 标签，同时提供离线备用版。新版需要新聊天，不兼容旧 LoreState 标签。离线 WishBook 转换工具是可选开发工具，不是运行时兼容层。

# 技术迁移验证

源文件：旧版世界书 SHA-256 `99a32575dc93acf63fb6f8335de2e871fccb004c962c565bb6ee6d6cdcd81183`。构建回执列出每个产物哈希。原档没有改写。

自动检查：43 项测试通过；其中新增检查 UID 0–5 的内容和全部元数据逐项相同、UID 8 叙事段落保留（只替换旧协议名）、脚本与世界书关联、HTML 三栏目一致、两条正则分别作用于显示/提示词、热冷档转换及冲突保护。JavaScript 语法检查通过。

导入包检查：角色脚本格式校验通过。通用组件校验器误将空 replaceString 判为非法；本包保留酒馆原生支持的空替换，已通过正则实例测试确认删除标签而保留正文，不使用空格或占位符规避校验。

浏览器检查：实际 DOMParser 校验 HTML 通过；使用同一 renderTemplate 渲染函数显示三个文字栏目，持续条件和关联可展开折叠。该检查是浏览器样式预览，不等于酒馆导入或模型语义验收。

宿主依据：依赖已在专用测试角色验证基础建档/冷藏/唤醒/增量/恢复的 LoreState prototype-v0.3.0。本次缄愿笔记整套组件尚未导入真实游玩卡；不自动停用用户旧 QR/正则，也不自动覆盖聊天。尚待新聊天实机导入、世界书实际触发、真实模型输出、角色卡及聊天导出再导入验收。

旧档转换工具仅生成离线预览并保留输入原文；没有收到具体旧聊天与冷档输入，因此不声称实际旧存档已迁入。当前冷档回归需要遵守先唤醒、下一轮更新的时序限制。
