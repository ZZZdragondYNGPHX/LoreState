# 验证边界

本轮将 WishNote 0.1.0 拆分为 LoreState 0.2.0 通用扩展与领域示例。

自动测试涵盖：任意领域路径、原子失败、字段保留、对象覆盖禁止、删除理由、JSON Pointer 转义、危险属性拒绝、结构和提示预算、历史回放与编辑失效、备份校验、旧愿档转换、宿主命名空间和生命周期。以实际 `npm test` 输出为准。

2026-09-06：23 项自动测试通过，JavaScript 语法检查和扩展 manifest 检查通过。在 SillyTavern 1.18.0 的独立测试聊天中实测 LoreState 0.2.0。扩展加载、打开面板、导入地点基线、预览未写盘、确认后独立命名空间写盘均通过。通过原生 `/sendas` 输入人工构造的 LoreStatePatch，将港口管理者从商会改为居民议会并新增交接事件；原地名保持不变。重载页面并重新打开聊天后，基线、修改和事件完整恢复。此项为真实消息事件与存储验证，不是模型生成验证。

旧 WishNote 0.1.0 曾在 SillyTavern 1.18.0 实测楼层保存、刷新、编辑、删除和 swipe。该结果是设计与宿主接口参考，不代表重构后的 LoreState 已通过所有相同场景。

尚需独立验收：LoreState 的真实模型生成及提示注入顺序、跨聊天完整操作回归、移动端、群聊、长会话、真实网络保存失败。没有进行付费模型调用，也未宣称用户已验收。
