# 当前工作：控制中心 UI 重构收尾 · 2026-09-11（本地未发布）

基底为 `main` / `01e92ccb88d45bf4bebdc191dde13fb12c82ed1b`。既有 UI 重构已收口为共享无状态组件：设置页五步折叠卡片、API 页面按任务分区、诊断/回档工具卡片化、页头与页签吸顶；保留原控件、事件处理器、配置键、数据协议和远程入口。补齐 44px 页签热区、原生复选框宿主主题防护、减少动态效果以及结构/窄宽回归。

`npm run build`、`npm run check`、99 项 Node、11 项模板浏览器和 58 项最终 bundle 模拟宿主检查通过；Edge 本地页已人工查看设置、诊断和 API 布局。生成物为 `artifact/bundle.js`、`prototype/dist/main/lorestate-script.json`、`prototype/dist/main/receipt.json`，bundle SHA-256 `8770421b5bec4c40e61004546bbf5bab2b02f51c5e436bcc8676aa569d68e623`，入口仍为 `@main`。未提交、推送、安装或发布；真实 SillyTavern 的主题、字体缩放、触控和 320px 设备是下一验收门，`realHostVerified=false`。

---

# 发布准备：0.10.0 · 2026-09-10

用户授权提交并推送 main 和新标签 0.10.0，确认发布版内置预设头尾留空，本地自定义文件另存仓库外并在推送后恢复为未提交修改。发布包含独立状态模型、API 预设、头尾 Base64 支持、同层续写、回档后更新及文档归档；远程入口继续跟随 @main，标签用于固定源码与 bundle。

空头尾版本重新通过 build、check、98 项 Node、11 项模板、57 项 bundle 模拟检查、组件校验及 diff 空白检查。生成 bundle、main 远程入口、receipt；真实酒馆和实际 API 尚未验收。此记录为提交前发布准备，最终 Git 推送结果以远程引用核验为准。

---

# 当前补充：内置预设 Base64 · 2026-09-10

头尾入口改为 `prototype/builtin-preset.js` 中的 `BUILTIN_PRESET_HEAD_BASE64` / `BUILTIN_PRESET_TAIL_BASE64`，默认留空。填写 UTF-8 标准 Base64（非保密加密），请求组装时解码；支持编码换行，拒绝非法 Base64、非规范填充及无效 UTF-8。固定任务和酒馆预设路径保持原样。使用说明提供编码方法。

build、check、98 项 Node、11 项模板及 57 项 bundle 模拟检查通过，远程三个产物已重建，ref 仍为 main。保留已有续写等修改；未提交、推送、安装或调用真实 API，下一门为真实宿主验证。以下为历史记录。

---

# 当前补充：同层续写与回档后额外更新 · 2026-09-10

用户授权补齐前述功能缺口。本地实现已支持最新 AI 回复的随正文/额外模型同层续写：从本层之前的状态计算，替换过时状态块；停止或关闭自动更新时保留正文并显示缺口，额外模式可手动补算。聊天中保存续写整理记录，支持重载、消息写入失败及完成标记保存失败后的恢复；分支改变时拒绝覆盖。复用现有回档基线，允许回档后的新回复额外更新、重算、撤销和续写；回档保留区不可原地重算、重抽或续写，任意历史批量重算仍未实现。

build、check、97 项 Node、11 项模板和 57 项 bundle 模拟宿主检查、组件校验及 diff 空白检查通过。`artifact/bundle.js`、`prototype/dist/main/lorestate-script.json`、`receipt.json` 已重建，`@main` 入口不变。未安装到真实酒馆或调用真实 API；流式/非流式实际时序、模型遵循率和其他扩展交互是下一验收门，未设置 driver-accepted。未提交、推送或发布，保留此前文档归档与内置预设头尾等修改。详情见[验证记录](prototype/verification.md)及[使用说明](docs/额外模型与API预设.md)。以下为历史记录。

---

# 当前补充：内置预设头尾加载 · 2026-09-10

按用户要求采用 MVU 类似的头尾组装：编辑 `prototype/builtin-preset.js` 的 `BUILTIN_PRESET_HEAD` / `BUILTIN_PRESET_TAIL`，由 `api-profiles.js` 调用组装函数，顺序为头部 → 固定状态任务 → 剧情 → 尾部。两端默认留空，不包含破限文本。使用本地 JS 模块构建加载，酒馆预设路径不叠加内置头尾。

build、check、96 项 Node、11 项模板、49 项 bundle 模拟检查通过；远程版三个产物已生成，ref 仍为 main。保留既有工作区修改，未提交、推送或安装；下一验收门为真实酒馆与 API 请求。下方单一 `BUILTIN_PRESET_PROMPT` 入口记录已被本次替代。

---

# 历史补充：内置预设命名与编辑入口 · 2026-09-10

已将 UI“内置状态整理”改为“内置预设”。新增 `prototype/builtin-preset.js` 的 `BUILTIN_PRESET_PROMPT`，默认留空，只作为内置预设的补充内容；固定状态规则/格式继续由 core.js 和 api-profiles.js 始终发送。正式 build 已接入该源文件，使用说明列明编辑与构建位置。95 项 Node、11 项模板及 49 项 bundle 模拟检查通过。未提交、推送、安装或调用真实模型，`@main` 入口不变；真实宿主验收仍待进行。

---

# 状态请求参数与两类提示词 · 2026-09-10

用户要求参考 MVU 配置适配文字状态脚本，并明确最终采用“内置提示词和酒馆提示词，内置不加破限”。本轮已在前次本地实现上接通内置状态整理、当前酒馆预设和指定酒馆预设；产品界面、提示词及当前说明统一称“状态”。内部旧存储键和宿主接口名保持兼容。

- 配置：自动更新开关、串行请求总次数 1～5、总超时 15～600 秒、流式接收；独立 API 或跟随当前 Chat Completion 连接；模型列表、最大 tokens、温度、Top P、Top K、频率/存在惩罚。旧绑定默认仍为内置、独立 API、自动、一次请求、120 秒。
- 提示词：无 MVU 破限文本，内置只做剧情状态整理。酒馆预设通过 `generate({preset_name})` 接入，完整状态任务放在本轮输入中，避免预设省略聊天历史占位时丢失格式要求；指定酒馆预设的采样参数按助手实现优先，界面与说明均已注明。
- 自动证据：95 项 Node、11 项模板及 49 项 bundle 模拟宿主检查通过；构建、语法、组件验证通过。覆盖参数保存/重载、两类预设请求、手动/自动、重试、取消、预设变化、模型列表迟到、术语及原有历史保护。
- 已生成 `artifact/bundle.js`、`prototype/dist/main/lorestate-script.json`、`prototype/dist/main/receipt.json`，仍为 `@main` 远程组件。未提交、推送、安装或调用真实模型；保留既有文档归档改动。源码版本及运行边界见[验证记录](prototype/verification.md)，操作见[使用说明](docs/额外模型与API预设.md)。
- 下一门仍为真实酒馆/API 验收。尚未验证真实流式、各模型采样参数兼容、酒馆预设最终组装与持久化。继续不支持同层续写、历史批量重算和额外模式快照回档；未增加并发重试、自动清理或 MVU 专用输出协议。未设置 driver-accepted。

---

# 历史工作：额外模型变量更新与 API 预设 · 2026-09-10

用户确认方案后授权本地实施，交付模式为 component。新增魔法棒控制中心的 API 预设页、全局本地预设与聊天独立绑定；正文结束后独立更新、最新 AI 回复重新更新、120 秒超时、取消和单次撤销。继续使用 v3 文字状态、原有字段校验与冷热档读取凭据，不引入 MVU 运行库。

- 源码：新增 `prototype/api-profiles.js`、`api-panel.js`、`extra-update.js`，调整 `runtime.js`、`core.js`、`control-center.js` 与既有构建。密钥只在全局专用命名空间；角色脚本和聊天只保留无密钥的模式/绑定信息。
- 自动化：92 项 Node 测试、11 项模板浏览器和 45 项最终 bundle 模拟宿主检查通过；包含独立生成重入核心钩子、错误响应、重试前态、取消、超时、写入失败、分支/聊天/规则变更和重抽。构建、语法和组件格式检查通过。
- 产物：`artifact/bundle.js`、`prototype/dist/main/lorestate-script.json`、`prototype/dist/main/receipt.json`。远程 ref 仍为 `main`，本次未提交或推送，远程/CDN 不包含这次未提交的新功能；未产生离线组件或新标签。
- 源码证据：本机 SillyTavern 1.18.0 / `51ad27fb86d39a3daca3adaa970375c9670c12df`，酒馆助手 4.9.5 / `af21bee2aadef5bf384f618e3456ff15ba9c431d`；参考 MVU `61010dab47bc3a08a1b626320bf7fc8c9573eca4`。未安装或运行本功能于真实酒馆，未调用实际模型。
- 边界：OpenAI 兼容接口；只重算最新 AI 回复；不支持同层续写和额外模式下的快照回档，不批量修复旧历史。前面有缺口时先修复。真实模型遵循率、实际 API 路由、流式完成时序、宿主持久化与其他扩展交互待实机验收；自动化未设置 driver-accepted。
- 使用说明：[额外模型与 API 预设](docs/额外模型与API预设.md)。下一门为真实酒馆与 API 验收，Git 提交/推送仍需单独明确授权。保留此前文档归档等未提交改动，未把历史发布授权沿用到本轮。

---

# 历史工作：仓库清理与远程版流程收口 · 2026-09-10

本轮目标：删除 `main` 的 `examples/`、删除实际存在的 `examples/wishnote` 本地/远端分支，并把活动构建改为只生成 `main` 远程入口。已确认 `main` 是滚动远程 ref；普通修复不需要修改 `package.json` 版本号，用户刷新脚本即可拉取推送后的最新 `artifact/bundle.js`。

当前交付边界：`npm run build` 生成 `artifact/bundle.js`、`prototype/dist/main/lorestate-script.json` 和 `prototype/dist/main/receipt.json`；不再生成新的离线版。旧固定版本目录及其历史离线产物不改写，只作为历史复现，不在活动文档中推荐。

已授权的 Git 操作：提交本轮 `main` 变更并推送 `origin/main`；删除本地和远端 `examples/wishnote` 分支。实际 SillyTavern、CDN 缓存刷新和用户验收仍是独立后续门，不由自动化代替。

本轮文档整理边界：活动文档保留在 `docs/`，5 个发布文件归并到 `docs/发布材料/`，旧迁移/旧扩展/旧验证记录归档到 `docs/archive/`；顶层 `release/` 不再作为项目目录。此项只改变文档路径和索引，不改变脚本、组件协议或历史 `prototype/dist/` 产物。当前尚未为这项整理提交或推送 Git。

---

# 历史工作：隐藏兼容修复与文档同步 · 2026-09-10

修复提交 `32df550` 已推送 origin/main。完整 `prototype-v0.9.2` 远程/离线组件正在由本轮分发补齐；`prototype-v0.9.0` 与仅脚本代码标签 `prototype-v0.9.1` 不改写。README、作者目录和发布材料以 0.9.2 稳定组件为入口，并提供 `main` 持续更新入口。

自动证据为 92 项 Node、44 项浏览器检查及问题聊天离线回放至第 8 楼零错误。实际酒馆安装及用户验收待完成；下一步按[隐藏助手兼容与更新](docs/隐藏助手兼容与更新.md)保留原配置替换代码并验收。当时的 `release/` 发布材料已同步安装链接；本轮已归并至 `docs/发布材料/`，附件内容保留。

---

# 0.9.0 Git 版本分发 · 2026-09-08

用户已授权推送本次模块更新。分发目标为 origin/main 与固定标签 prototype-v0.9.0；published=true 表示本次 Git 分发，realHostVerified=false 保留。未安装脚本、调用模型或创建 GitHub Release。下方本地实施记录作为历史阶段证据保留。

---

# 当前工作：0.9.0 模块条目已本地实装 · 2026-09-08

用户批准第一版模块更新，并明确不需要兼容任何旧数据。本次 component 交付完成模块声明、分类栏目、按需提示规则与正文后统一更新；新作者入口要求【LoreState模块 v1】，不转换旧条目或迁移聊天。

- 新增 prototype/modules.js、module-example.txt、module-example.html；核心解析、提示组装、HTML 校验与分类渲染、设置保存/预览、预设、冷档显示均已接通。首次规则全量，后续按完整档案类别/输入模块名加载，目录常驻，预算裁剪同步收回详细规则与冷档权限。
- 自动证据：89 项 Node、11 项模板浏览器、32 项最终 bundle 模拟宿主检查通过；语法、构建、导入组件格式、内容哈希及本地文档链接通过。完整边界见 prototype/verification.md 顶部。
- 产物：prototype/dist/v0.9.0/ 与 artifact/bundle.js。用户入口：[0.9.0 模块条目与试卡](docs/0.9.0模块条目与试卡.md)。旧发布产物与既有未跟踪 release/ 未改动。
- 限制：未选中模块的突发新实体按目录/通用规则建档，专用规则下一轮生效；同名字段约束仍跨模块共用；合计最多 32 个不同实体栏目；没有额外模型调用或语义检索。
- published=false / realHostVerified=false，未设置 driver-accepted。未安装、提交、推送或发布。下一关为用户在真实酒馆的新测试卡、新配置与新聊天中统一试卡；不把模拟宿主检查当成实机验收。

---

# 0.8.0 远端分发 · 2026-09-08

用户已授权推送本次实现；分发目标为 origin/main 与固定标签 prototype-v0.8.0。构建 published=true 表示本次 Git 版本分发，realHostVerified=false 保留。实机测试仍由用户统一完成；未创建 GitHub Release，也未安装脚本。旧 release/ 为既有未跟踪材料，不包含在本次提交中。下方保留本地实施阶段的历史记录。

---

# 0.8.0 剩余改进已本地实装 · 2026-09-08

用户要求继续实装其余建议，并在全部实装结束后统一实机测试。本次保持 component 交付，完成同层续写拦截、快照正文/配置去重与容量显示、可选作者初始档案、必填/禁止删除/枚举约束，以及冷档当轮读取凭据；保留 0.7.1 的失败中止。

- 输出 prototype/dist/v0.8.0/ 和 artifact/bundle.js。0.7.0、0.7.1 产物保留，远程 prototype-v0.8.0 尚未发布。没有安装、模型调用、Git 提交、推送或发布。
- 新增 prototype/snapshot-store.js；状态正文与配置按 SHA-256 键共享，所有快照和读取凭据保留。旧 snapshots 无损转存后不重复保存旧数组。新格式不可由旧脚本读取，回退必须恢复升级前聊天备份。
- 作者默认配置存脚本 authorPolicy，生效配置冻结在聊天 policy。已有历史不能从设置页改初值/约束；空聊天可预览后应用。模型生成外层可携带 read UUID，回放核对完整前态、配置与已加载实体清单。无 read 的旧标签仍可用，但冷档保持空唤醒保护。
- 自动验证：79 项 Node、9 项模板浏览器、29 项最终 bundle 模拟宿主检查通过；构建、语法、组件格式与内容一致性校验通过。5 万字不变资料、200 层及 200 条读取凭据：序列化从 30,413,477 字节降至 261,307 字节；不代表持续更新场景有同等收益。
- 用户入口：[0.8.0 使用与统一验收](docs/0.8.0使用与统一验收.md)，包括可复制初值/约束与实机测试表。下一关由用户统一验证真实生成、流式/非流式中止、持久化、重抽/删尾/回档和配置隔离。
- realHostVerified=false，未设置 driver-accepted。仍无增量检查点回放、自动删除历史、额外模型调用或同层续写合并。工作区原有 release/ 未修改。

---

# 0.7.1 状态准备失败中止生成 · 2026-09-08

用户在 MVU 对照审查后授权实装；本次实施上一轮选定的第一项局部修复，交付为脚本 component。世界书读取/关联、提示预算、回档校验或注入失败时，调用 SillyTavern 的 stopGeneration()；过期聊天失败不停止新聊天。接口失败时明确要求手动停止，不虚报成功。

- 输出：prototype/dist/v0.7.1/lorestate-script-offline.json 与 artifact/bundle.js；原 0.7.0 产物保留。脚本 ID、按钮、data 和导出策略保持一致；升级时保留用户原脚本配置。
- 验证：67 项 Node、9 项模板浏览器、22 项最终 bundle 模拟宿主检查通过；构建、语法、组件格式及内容一致性校验通过。新增失败测试在修复前确实失败。
- API：核对本机 SillyTavern 1.18.0 源码提交 51ad27fb86d39a3daca3adaa970375c9670c12df；未启动/安装真实酒馆或调用模型。细节见 prototype/verification.md。
- 未提交、推送、安装或发布。远程 prototype-v0.7.1 仅预留，使用离线组件测试；published=false / realHostVerified=false，未设置 driver-accepted。
- 下一关：独立测试聊天核验流式/非流式请求实际停止、失败修复后恢复。续写、快照去重、作者初始档案、字段约束和冷档当轮更新仍未实现。

---

# 0.7.0 推送与作者文档 · 2026-09-07

用户已授权推送 main 和本次实现，并要求 docs 作者文档。新增目录与五篇教程，覆盖创作规则、两个完整示例、HTML 和排错。67 项 Node、9 项模板浏览器、13 项模拟宿主检查通过。目标标签 prototype-v0.7.0，published 标志用于本次 Git 版本分发；realHostVerified 和 driverAccepted 保持 false。CDN、真实酒馆安装与游玩仍待验收，未创建 GitHub Release。

下方是推送前的实施记录。

---

# 当前工作：0.7.0 世界状态冷热档候选 · 2026-09-07

用户已授权实装，并明确不考虑旧数据兼容。本次仅本地源码、测试与脚本组件构建，未安装、提交、推送或发布。

- 当前入口仍为 prototype/。core.js 使用 v3 / Entity / entities / schema.entity，命名空间 lorestate_world_v3；旧 Person、旧模板与旧状态明确拒绝。
- Entity 支持类别、冷热、最多 8 项有向关联、活动事件标记、剧情确认时间及事实来源楼层；Shared 常驻且只写变化。
- 一跳最多额外取回 8 个实体，最多 24 条冷档索引；预算不足撤下可选完整档案，不截断必需资料。全量存储与历史快照保持完整。
- prototype/world-memory.md 是本版协议与使用说明。scripts/test-browser.mjs 为 Edge 模拟宿主回归入口。
- 旧 wishnote 示例固定到 examples/wishnote/legacy/state-v2.js，避免旧版本转换工具依赖现行协议；v3 不包含该模块或旧数据迁移。
- 构建输出 prototype/dist/v0.7.0/ 和 artifact/bundle.js；版本收据 published=false / realHostVerified=false。远程入口只是待发布配置。
- 下一关：新测试聊天导入离线组件，进行真实酒馆、实际模型和用户游玩验收。字段级冷热、语义召回和后台自主模拟均未实现。

下方保留历史交接，出现版本冲突时以本节及 prototype/verification.md 的 0.7.0 记录为准。

---

# LoreState 当前交接

最新：0.5.2 修复正文状态栏尺寸，新增 prototype/state-frame.js，输出 prototype/dist/v0.5.2/。52 项 Node 和 10 项模拟浏览器检查通过；用户主题下实机复测待完成。用户授权直接推送 main 与 prototype-v0.5.2。

最新：prototype 0.5.1 单入口控制中心 UI 重构，用户已授权推送 main 与 prototype-v0.5.1 标签。源码新增 prototype/control-center.js，构建输出 prototype/dist/v0.5.1/。52 项 Node 与 9 项模拟宿主浏览器检查通过；新 UI 尚未真实酒馆验收。下方 0.5.0 记录作为前版功能背景保留。

更新：2026-09-07。当前本地工作为 prototype 0.5.0 诊断与历史管理器候选版，发布与验证以 prototype/verification.md 为准。用户批准错误诊断、基础修复、历史状态管理器的脚本组件实现。用户已授权推送 main 与 prototype-v0.5.0 标签；未安装至用户游玩卡。

## 本次交接

- core.js 新增行列／作用范围诊断、连续正常楼层、历史缺口、按层查看、实际变化对比、独立 & 修复建议。仍按消息原文重放，失败整轮不提交，后续有效更新继续应用但明确标记缺口。
- runtime.js 新增魔法棒管理器、可关闭去重告警、诊断复制、校验后格式修复、聊天内单次撤销备份；写回前校验聊天／swipe／历史／schema／起点。生成期间暂缓刷新，结束后复验。
- 构建 `node scripts/build-prototype.mjs` 输出到 `prototype/dist/v0.5.0/`；离线候选版为测试入口，远程 ref 固定 prototype-v0.5.0。旧 dist 根目录、缄愿笔记示例仍为 0.4.0，未随本次升级。
- 自动检查及真实酒馆结果见 verification.md；历史、诊断、修复写回、重载及撤销已实测。下一步由用户亲自游玩测试，流式生成、重抽、删尾及长聊天边界仍待验证。

## 结构

- prototype/core.js：唯一 applyState / replayState / playPrompt，状态为 `{version:2,shared:{},people:{}}`。
- prototype/template.js：同一份 HTML 的公共栏目＋一个 data-lore-person 重复容器；自动推导 shared/person schema。
- prototype/runtime.js：无 memoryMode；脚本与聊天各自的 lorestate_unified_v1 命名空间。
- prototype/presets.js：只保存新预设，分别校验两种栏目范围；无旧 HTML 自动迁移。
- examples/wishnote/people/：缄愿笔记统一版组件，schema 为空公共栏目＋三个人物栏目，世界书 UID 0–5 不变。people 目录表示人物题材示例，不是独立引擎。
- 原 people.js 已合入 core.js；根目录 src/manifest 是历史原生扩展，不参与新的脚本运行。

## 产品边界

酒馆助手固定前提，作者文字规则＋网页 AI 制作 HTML＋随卡脚本配置。最新回复显示完整状态，首次 full，之后 delta。离场原文保留本地，模型通常只见索引；输入明确提及时预取；空 delta 唤醒后下一轮更新。不做额外模型调用、MVU 数值系统或旧协议兼容。

消息原文是权威，快照只供读取。手动编辑标签后重新读取可纠错，不能自动发现后续剧情语义冲突。配置和 HTML 随卡，聊天记录不随角色脚本导出。

## 构建与检查

依次运行 node scripts/build-prototype.mjs、node scripts/build-wishnote-people.mjs、npm test、npm run check。浏览器绑定回归在 tests/template-browser.html；已测与未测范围见 prototype/verification.md。

旧清理工作留下的 docs/开发说明.md、旧交接归档和 release/ 不应当被误删；以上是当时的历史状态，旧交接现已归档、release/ 已归并，旧 examples/wishnote worktree 也已清理。../archive/wishnote-legacy/ 仍为原件归档。


## 2026-09-10 · Entity mode 诊断与生成提示修复（本地未发布）

- 用户授权：补充报错日志，定位并减少 AI 混淆 full/delta；仅修改 prototype/core.js、新增 tests/entity-mode.test.mjs 并通过正式 build 生成 main 产物。已有文档整理改动保留。
- 证据：提供的第 10 楼新实体缺少 mode；旧只读资料使用无 mode 的 Entity 标签，形成与更新语法相似的示例。此处是可观察的混淆源，不作为已证明的模型内部原因。世界书未发现 Delta 定义；mode 规则由脚本 preparePrompt 注入。
- 修复：只读资料改为 EntityRecord；区分外层和实体 mode，要求小写，说明 delta 外层可包含新实体 full。诊断新增 ENTITY_MODE、entityId、entityExists、actualMode、expectedMode，提示明确修复位置；继续严格拒绝错误整轮，不静默补值。
- 自动化：87 项 Node 测试、11 项模板浏览器和 33 项最终 bundle 模拟宿主检查通过；npm run build、npm run check、git diff --check 通过。私人附件仅内存回放，未加入仓库。
- 产物：artifact/bundle.js、prototype/dist/main/lorestate-script.json、prototype/dist/main/receipt.json；远程 ref 为 main。未提交、推送、安装或进行真实模型调用；真实酒馆及模型漏写率待验证。旧第 10 楼仍需明确补全原文后重新校验。
