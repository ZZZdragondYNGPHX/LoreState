# LoreState 接手说明

主线是可直接安装的 SillyTavern UI 扩展，当前版本 0.2.0。缄愿笔记已归入 `examples/wishnote`，地点笔记 `examples/world-notes` 用于证明领域独立。

核心入口为 `src/core.js`，状态采用 `{schemaVersion, profile, data}`。`data` 只允许对象与文字；元数据和迁移原件不进入模型上下文。协议是 LoreStatePatch，路径相对于 data。命名空间为 lorestate_v1，旧 wishnote_v1 不会自动转移。

`src/host.js` 延续旧版经过验证的原生宿主接口，但本轮真实测试仅覆盖加载、预览、建立基线、人工消息更新和重载恢复。23 项自动测试通过；真实模型、群聊、移动端与长会话仍待验证。

明确限制：当前完整注入，不提供冷档选择；profile 为提示规则，不是可执行领域验证器；不支持同层续写；备份恢复建立新基线而非恢复整段聊天。不要把这些边界写成已经支持。

后续如扩展冷热投影或字段约束，应先定义独立于 wishnote 的通用配置，并增加第二领域用例。不要把 wishes、恒/程或 P 编号放回主线内核。
