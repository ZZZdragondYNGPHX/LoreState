# 当前交接 · LoreState 0.13.1 · 2026-09-16

本轮修复 `LoreStateReview` 在作者使用 `当前时间` / `当前地点` 等自定义公共栏目时被硬编码示例诱导成 `/shared/时间`、`/shared/地点`，导致每次额外状态模型请求都在本地校验阶段失败的问题。

## 基底与交付

基底为 `main` commit `bc6a8d7c8076026dc6e806a066364a2c5b595cb1`。修复从短期 `fix/state-review-author-rules` 开发，验收后快进 `main` 并创建不可移动标签 `0.13.1`。

- `state-review.js`：移除硬编码业务核对重点与 `/shared/地点` 示例，动态列出本轮 schema 的精确必查/条件 path；未完整加载冷档仅允许 presence。
- `core.js`：状态模型明确以状态栏条目 + 自定义更新核对规则作为唯一字段更新判断规则来源。
- `extra-update.js`：重试明确要求逐字复制合法 path。
- `api-profiles.js`：额外模型任务不再附加新的字段更新语义。
- `tests/state-review.test.mjs`：覆盖 `当前时间` / `当前地点`、非法缩写、动态路径和冷档边界。

## 兼容性

XML v3、模块 v1、Template API v2、作者默认档案、现有状态/快照/回档均不迁移。0.13.0 可直接升级。

## 验证

自动发布流程执行 main/0.13.1 构建、check、Node 测试、browser 测试（含 EJS runtime 页面）。真实 SillyTavern / 真实模型 / 手机人工验收未执行，`realHostVerified=false`。
