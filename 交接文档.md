# 当前交接 · LoreState 0.12.3 · 2026-09-15

用户授权：实现配置删除、多协议 API、思考程度及默认值调整，提交推送 main 并创建不可移动标签 0.12.3。基底为 origin/main / d7091ceccb164dbd04f440413956980a2188d682（0.12.2）；本地原先落后远端，已先整合远端更新。

## 本次交付

- 规则配置 → 聊天维护 → 输入“删除旧配置” → 彻底删除旧配置。清除本脚本配置与本聊天状态/快照/绑定；保留正文、世界书、全局 API。旧聊天通过配置代次隔离，重新启用才清理并从新起点更新。重置后可先绑定 API 制作 HTML。
- 保留原有 Helper 连接，新增 Chat Completions、Responses、Anthropic 直连；支持任意基础前缀、完整端点及查询参数、自定义请求头和同源模型列表。
- 当前/指定酒馆提示词预设经 Helper 组装后按请求编号中止发送，再由新协议发送。未调用真实服务验证；跨域和宿主事件组合仍待实机验收。
- 默认 3 次总请求、0 无限等待、8000 tokens、auto；支持显式 max / ultra 等级且不降级。实际供应商支持范围由服务决定。
- 146 项 Node、145 项浏览器（29 模板 + 103 bundle 宿主 + 13 EJS）通过；build/check 与生成物一致性通过。自动化未设置 driver-accepted。

## 产物与边界

artifact/bundle.js；prototype/dist/main/ 与 prototype/dist/0.12.3/ 各包含远程脚本 JSON 和 receipt。main 入口仅引用 @main/artifact/bundle.js；固定入口仅引用 @0.12.3/artifact/bundle.js。旧标签不移动；不创建 GitHub Release，不安装到真实酒馆。

bundle SHA-256：80dae6047a5b0cd2f658385c0abe87b44c85184ff13be090c111b839bb6e0276。提交和标签必须指向同一交付状态，远端推送结果以最终 Git 引用核验为准。

工作前已有的 prototype/builtin-preset.js 私人本地改动在仓库外按原字节备份，发布构建使用受版本控制的公共文件；发布后恢复私人改动，不能将其打进公共 bundle 或提交。

下一验收门：在独立真实测试聊天验证删除/重配、旧聊天隔离、三协议连接及当前/指定预设、思考等级、流式与取消。详见 [发布说明](docs/发布材料/0.12.3-配置重置与多协议API.md) 和 [验证记录](prototype/verification.md)。

---

# 历史交接 · LoreState 0.12.1 · 2026-09-14

当前固定稳定版为不可移动标签 **`0.12.1`**；`main` 是唯一长期开发分支。正常用户安装固定版，只有明确需要跟随开发时才使用 main。

本轮进行了**文档同步**，没有修改 `prototype/` 运行维护源、`artifact/bundle.js` 或固定版本产物。目标是清除仍把 0.7～0.12.0 当“当前版”的说明，并把作者流程统一到 0.12.1。

## 当前 0.12.1 功能状态

- XML v3 状态协议：首次 full、后续 delta；错误轮原子拒绝。
- `【LoreState模块 v1】`：公共栏目与人物/物品/国家/事件等模块拥有各自字段。
- 热档/冷档、按需读取、read receipt、活动事件与有限一跳关联。
- Template API v2：多区域、部分/重复字段、自定义冷档布局。
- 0.11.1 起：API 辅助外观制作、条件显隐、字段值 → class 映射。
- 0.12.0 起：可选 ST-Prompt-Template / EJS 只读桥接。
- **0.12.1：首次设置新增“确认绑定状态栏条目”，刷新/切页保留选择，首次 HTML 制作读取正确条目。**

## 当前首次使用流程

1. 使用 [0.12.1 固定远程组件](prototype/dist/0.12.1/lorestate-script.json)，只启用一份 LoreState。
2. 在世界书创建/选择 `【LoreState模块 v1】` 状态栏条目。
3. 打开设置，选择世界书与状态栏条目。
4. 点击 **确认绑定状态栏条目**。
5. 打开外观制作，通过 API 生成/修改 HTML，或粘贴合法 Template API v2 模板。
6. 预览并应用外观；首次使用回设置点击 **保存 HTML 并启用本聊天**。
7. 用独立测试聊天验证 full → delta、刷新/切聊天、冷热档和导出。

确认绑定本身只保存准备使用的世界书/条目，不启用聊天、不写状态、不改变世界书活动规则。

## 当前版本与升级边界

- `0.11.0 / 0.11.1 / 0.12.0 → 0.12.1`：合法 v2 HTML、v3 状态、聊天快照和模块配置无需迁移。
- `0.10.6` 或更早 → 0.12.1：旧 HTML 原文会保留，但当前展示层需要重新制作 Template API v2 外观。
- `0.12.0` 是 EJS 桥接首发版本，不是当前推荐固定安装版本；当前固定安装仍是 `0.12.1`。
- 旧 Git tag 不移动，不创建裸语义版本 branch；下一正式修复应发新 patch tag。

## 当前验证事实

0.12.1 发布记录确认：

- 构建、语法和版本守卫通过；
- 132 项 Node 测试通过；
- 29 项模板浏览器检查通过；
- 94 项最终 bundle 模拟宿主检查通过；
- 13 项 EJS bundle 检查通过；
- 浏览器检查共 136 项。

模拟宿主覆盖非第一条目、刷新、设置 ↔ 外观双向切页、独立绑定、配置序列化恢复、首次 HTML 生成和条目删除。

**真实 SillyTavern、实际模型、手机实机和长期人工游玩尚不能由这些自动化代表。** 当前发布边界仍保持 `realHostVerified=false`。详细见 [当前验证记录](prototype/verification.md)。

## 本轮文档同步结果

已更新为当前 0.12.1：

- 根 [README](README.md)
- [docs/README.md](docs/README.md)
- [作者入门](docs/作者入门.md)
- [状态栏条目创作指南](docs/状态栏条目创作指南.md)
- [状态栏条目与连续更新示例](docs/状态栏条目与对话示例.md)
- [Template API v2 指南](docs/HTML模板适配指南.md)
- [外观制作台](docs/外观制作台.md)
- [EJS / 动态世界书](docs/EJS动态世界书.md)
- [额外模型与 API 预设](docs/额外模型与API预设.md)
- [隐藏助手兼容与更新](docs/隐藏助手兼容与更新.md)
- [常见问题与试卡清单](docs/常见问题与试卡清单.md)
- [开发说明](docs/开发说明.md)
- [版本管理](docs/版本管理.md)
- [prototype 当前入口](prototype/README.md)
- [当前验证记录](prototype/verification.md)
- [发布材料 README](docs/发布材料/README.md)
- [Discord 帖子](docs/发布材料/Discord帖子.md)
- [作者改造教程](docs/发布材料/作者改造教程.md)
- [状态栏条目附件](docs/发布材料/状态栏条目.txt)
- [Template API v2 模板附件](docs/发布材料/状态栏模板.html)

历史 `0.8.0` / `0.9.0` 文档已明确标注历史用途；带明确版本号的 `0.10.6`、`0.11.0`、`0.11.1`、`0.12.0` 发布说明继续保留当时事实，不强行改成 0.12.1。

## 当前开发入口

Node.js 22+：

```sh
npm run build
npm run check
npm test
```

涉及 UI/模板/宿主时补：

```sh
node scripts/test-browser.mjs
```

涉及 EJS 时按需要补：

```sh
node scripts/test-ejs.mjs
```

当前 0.12.1 已发布；下一 patch 的构建示例统一写为：

```sh
npm run build -- 0.12.2
```

正式固定 loader 必须引用自己的 `@<version>`，不能引用 `@main`。

## 下一步开发者接手顺序

1. 读根 `AGENTS.md`。
2. 读 [版本管理](docs/版本管理.md)。
3. 读 [开发说明](docs/开发说明.md)。
4. 根据任务读 [当前验证记录](prototype/verification.md) 与对应发布说明。
5. 若改作者/模板行为，同时检查作者入门、Template API、发布材料是否需要同步。
6. 若改 EJS，保持只读、同一可靠回放事实源和空态边界；不得另建变量系统。
7. 若改首次设置流程，保留 0.12.1 的显式条目确认，不得重新引入“默认第一条”的隐式绑定。

历史候选、旧“当前工作”和阶段性哈希可通过 Git 历史读取；本文件只维护真正的**当前交接状态**，避免下一位开发者被旧标题误导。
