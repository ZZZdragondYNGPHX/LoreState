# 当前交接 · LoreState 0.12.3 · 2026-09-15

本文件只保留**当前接手所需事实**。旧交接全文不再叠加在当前文件中；需要追溯时查看 Git 历史与 `docs/archive/`。

## 当前版本

- 当前固定稳定版：不可移动标签 **`0.12.3`**。
- 当前维护线：`main`；维护源在 `prototype/`。
- 发布提交：`817b4bfdaed9e10b4d16f22e9d1347aee6ded643`。
- 基底：`d7091ceccb164dbd04f440413956980a2188d682`（0.12.2）。
- 固定入口只引用 `@0.12.3/artifact/bundle.js`；main 入口只引用 `@main/artifact/bundle.js`。
- 旧 tag 不移动，不创建语义版本 branch，不发布 GitHub Release 作为当前流程的一部分。

## 0.12.3 交付

- **配置重置**：规则配置 → 聊天维护 → 输入 `删除旧配置` → 彻底删除旧配置。清除本脚本配置和当前聊天状态/快照/回档/更新绑定/恢复备份；保留正文、世界书、全局 API、其他命名空间和显示过滤。其他旧聊天通过配置代次隔离，重新启用时再清理旧状态。
- **多协议 API**：保留 Tavern Helper OpenAI 兼容路径，新增 Chat Completions、OpenAI Responses、Anthropic Messages 直连；支持基础前缀、完整端点/查询参数、自定义请求头和同源模型列表。
- **酒馆预设桥接**：当前/指定酒馆提示词预设可由 Helper 组装，在真正网络发送前按请求标识中止，再转由目标原生协议发送；缺少桥接能力时明确失败，不静默回退内置预设。
- **默认值**：3 次总请求、0 无限等待、8000 tokens、`auto`；支持显式 `none`、`minimal`、`low`、`medium`、`high`、`xhigh`、`max`、`ultra`，是否受支持由实际服务决定。

## 当前验证

- Node：146 / 146。
- 浏览器：29 Template + 103 bundle 宿主 + 13 EJS = 145。
- bundle SHA-256：`80dae6047a5b0cd2f658385c0abe87b44c85184ff13be090c111b839bb6e0276`。
- `realHostVerified=false`；未安装到真实用户酒馆，未把自动化标成 driver-accepted。

完整验证入口见 [prototype/verification.md](prototype/verification.md)。

## 下一验收门

在独立真实测试聊天验证：

1. 删除/重配与旧聊天隔离；
2. Chat Completions / Responses / Anthropic 的真实连接、SSE、取消和迟到保护；
3. 当前/指定酒馆提示词预设桥接；
4. 不同模型对思考等级/采样参数的真实支持；
5. full → delta、冷热档、回档、隐藏楼层和 EJS；
6. 手机窄屏与长期聊天。

## 接手顺序

1. 读 [AGENTS.md](AGENTS.md)。
2. 读 [版本管理](docs/版本管理.md)。
3. 读 [开发说明](docs/开发说明.md)。
4. 根据任务读 [当前验证记录](prototype/verification.md) 和对应版本发布说明。
5. 改作者行为时同步检查 `docs/README.md`、作者入门、FAQ 与发布材料。
6. 改固定版本时始终从精确 tag/ref 取证，不用当前 main 反推历史。

[0.12.3 发布说明](docs/发布材料/0.12.3-配置重置与多协议API.md) · [文档目录](docs/README.md)
